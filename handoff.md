# QiDao 交接说明

## 我们在做什么

QiDao 是一个 macOS 围棋编辑与 KataGo 分析应用。本轮处理的是普通“分析”模式的响应问题：棋盘已经更新时，AI 候选有时迟迟不显示；点击 AI 设置后结果却立即出现。目标是让当前局面优先于后台全局胜率扫描，并让首个有效结果在应用失焦时也主动刷新界面。

## 完成了什么

- 根因一：普通分析只取消 Swift 任务，没有在提交当前局面前向 KataGo 终止仍在运行的 `fullscan-*` 查询；后台扫描会继续占用引擎。
- 根因二：普通分析使用 500 ms 防抖、优先级 10，并可能按 1 秒配置等待阶段结果；实时分析已有更快路径，但普通分析没有共用。
- 根因三：首个结果的强制异步窗口刷新只对实时识别生效。普通结果已经进入 `BoardViewModel`，但失焦窗口可能直到设置按钮等下一次 UI 事件才绘制。
- `26ef6cd` 增加协议回归：假 KataGo 在全局扫描未终止时拒绝返回普通候选。
- `eda0788` 增加展示回归：记录普通分析首结果是否以 `force=true` 请求刷新。不要用 AppKit 自发的布局/重绘次数作断言，它们有噪声。
- `b943445` 完成修复：普通/实时当前局面统一为交互优先级；普通防抖改为 120 ms；每次交互查询前显式终止全局扫描；普通阶段结果间隔最多 250 ms；普通和实时共用首结果刷新门；普通首结果到达后再恢复后台全局扫描。
- 针对性 Swift 冒烟均通过：协议测试确认全局扫描先终止、优先级为 30、普通候选五秒内返回；刷新测试确认普通首结果触发强制异步刷新，且没有同步 `displayIfNeeded`。
- 全量自动验证：Python 视觉测试 94/94，Rust 41/41；仓库、CI 策略、发布审计和空白检查全部通过。Python 采集时限测试不要与 Rust 首次全量编译并行运行，否则 CPU 争用会造成假超时。
- 本地 App 已重新构建；临时解锁项目专用签名钥匙串后，`codesign` 确认应用磁盘有效并满足指定要求。

## 卡在哪儿

自动化没有阻塞。最后一次真实普通分析失焦界面验收尚未完成，因为 Mac 在界面控制阶段锁屏；不能据此宣称真实 UI 场景已验收。

用户已于 2026-08-20 明确授权提交并执行普通 fast-forward push。推送后新会话仍应先用 `git status -sb` 和 `git rev-list --left-right --count origin/main...HEAD` 复核远端状态；禁止 force push。

## 下一步计划

1. 解锁 Mac，只启动当前仓库构建的 QiDao。
2. 在普通“分析”模式启动已配置引擎，在 QiDao 本地测试棋盘落一子，随后让 Chrome 保持前台至少五秒。
3. 确认候选和引擎状态无需打开设置即可出现，QiDao 没有抢焦点；同时采样进程，确认没有同步 `displayIfNeeded` 阻塞。
4. 若验收通过，把本计划剩余复选框标记完成，更新本交接的“卡在哪儿”，提交文档。
5. 本轮已经获得更新远端授权；只允许普通 fast-forward push，禁止 force push，也不要上传本地审计分支。

## 采过哪些坑不要再踩

- 不要靠激活 QiDao、模拟点击设置按钮或定时轮询来“唤醒”结果；那只会掩盖状态发布和窗口刷新边界的问题。
- 不要调用同步 `displayIfNeeded`。QiDao 失焦时它可能等待 RenderBox/WindowServer，反过来饿死识别与分析消息。
- 取消 Swift `Task` 不等于终止 KataGo 查询；切换当前局面时必须发送对应 `terminate`。
- 不要在当前局面首结果出现前启动全局扫描；两者共享 KataGo 神经网络批次，低优先级查询仍会增加延迟。
- 不要把首结果等待状态限定为实时识别；普通分析也需要一次强制异步窗口失效。
- 不要让过期节点或空结果清除首结果等待状态，也不要因此恢复全局扫描。
- 不要用 AppKit 自动产生的 `setNeedsDisplay` 或 `layout` 次数作为业务刷新证据；测试应验证 `refreshLiveWindowsIfNeeded(force: true)` 的边界契约。
- 不要把 Python 采集进程的严格超时测试与 Rust 首次全量编译并行运行。
- 不要提交模型权重、KataGo 大模型、签名材料、生成 Core、构建产物或本地审计分支。
